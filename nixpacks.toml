providers = ["node"]

[phases.setup]
nixPkgs = ["nodejs-18_x"]

[phases.install]
cmds = ["npm install --force"]

[phases.build]
cmds = ["npm rebuild && npm run build:client"]

[start]
cmd = "npm start"