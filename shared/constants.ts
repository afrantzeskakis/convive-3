// Define all shared constants here

// Default threshold for "high roller" dinner check average (per person in $)
export const DEFAULT_HIGH_CHECK_THRESHOLD = 150;

// Message expiration time in days
export const MESSAGE_EXPIRATION_DAYS = 30;

// Max file upload size in bytes
export const MAX_FILE_UPLOAD_SIZE = 10 * 1024 * 1024; // 10MB

// Default pagination limits
export const DEFAULT_PAGE_SIZE = 10;